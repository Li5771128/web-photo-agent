## Agent skills

### Issue tracker

任务与规格以 Markdown 存放在 `.scratch/`。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认分诊标签词表。详见 `docs/agents/triage-labels.md`。

### Domain docs

采用单一上下文的领域文档布局。详见 `docs/agents/domain.md`。
