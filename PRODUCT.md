# 参考图调色 Agent

## 1. 产品定位

面向使用 Lightroom 修图的摄影爱好者，提供由参考图驱动、以手动调色为核心的审美分析与参数建议服务。账户用于保存用户主动确认的审美偏好与项目结论，而不是默认囤积原始照片。

用户上传一张喜欢的参考图 A 和一张待调整的目标照片 B。Agent 分析 A 的视觉风格与色彩结构，比较 A、B 的差异后，生成针对 B 的、可解释且可执行的 Lightroom 调色建议。产品不替用户直接生成成片；用户保有最终控制权，并可以按建议手动调整或选择导出 Lightroom XMP 预设作为起点。

## 2. 目标用户

- 以 RAW 为主要修图格式、使用 Lightroom 的摄影爱好者。
- 偶尔使用醒图等手机修图软件，希望获得简化调色建议的普通拍照用户。

首版优先服务 Lightroom 用户。醒图只复用同一份调色意图，输出简化的参数映射；不承诺与 Lightroom 参数一一等价。

## 3. 要解决的问题

现有滤镜需要逐个试用，结果常不适配当前照片；一键 AI 修图则不可控、难学习，也可能需要反复生成。

本产品把“我喜欢这张图的感觉”转换成：

1. 可视化、可理解的风格分析；
2. 针对目标照片的分步骤调色计划；
3. 有原因、范围和风险提示的具体参数；
4. 可选的 XMP 预设下载。

## 4. 首版范围

### 支持的输入

- 参考图 A：单张本地上传的 JPG、JPEG、PNG 或 WebP。
- 目标照片 B：单张本地上传的 RAW，或 JPG、JPEG、PNG、WebP。
- 一次分析固定为一张 A 对一张 B。

### 账户与审美记忆

- 首版使用邮箱验证码登录。
- 新用户只使用 A、B 进行分析；在 2–3 次主动确认偏好后，才开始提供轻量个性化建议。
- 用户可保存全局偏好，以及人像、风景、夜景、街拍等场景偏好档案。Agent 仅可建议场景，用户确认后才启用该场景档案的强约束。
- 偏好分为“必须避免”“通常喜欢”“可尝试”三种强度。参考图 A 永远优先，个人偏好只作为二级约束。
- Agent 只在用户明确确认后写入偏好；每条偏好必须可查看来源、编辑、停用、删除和恢复上个版本。

#### 偏好档案与生效闭环

- 场景档案由用户创建，或由用户在某次任务中确认候选偏好后创建；空档案可存在，但不产生约束。档案开关只控制其是否可在后续任务中被采用，不代表已在当前任务生效。
- 一次任务只可采用全局偏好或一个已确认的场景档案。工作台应清晰展示本次采用的档案、其中启用的偏好和未采用原因；未创建、已停用或为空的场景档案不得伪装为强约束。
- 对每条实际参与规划的偏好，系统必须说明其影响的参数、建议值或安全范围；参数卡应标明“参考图驱动”“偏好修正”或“参考图优先，偏好未采用”。用户可查看关闭个人偏好与开启个人偏好两组方案的差异。
- 任务反馈（采纳、修改后保留、不喜欢）仅作为候选偏好的证据，不自动写入。系统可生成候选偏好，并要求用户明确选择保存位置（全局或场景）、强度和内容后才写入。
- 候选生成必须区分反馈语义：直接采纳可默认生成正向候选；“修改后保留”必须先记录用户将推荐值调强、调弱或缩窄范围等具体修正，不能以固定偏好代替；“不喜欢”必须先由用户指出不喜欢的具体方面，并默认写为“必须避免”，不得从负向反馈机械推断为“通常喜欢”。
- 参考图与“通常喜欢”或“可尝试”冲突时，参考图优先并记录未采用原因；与“必须避免”冲突时，优先收紧高风险参数范围，并说明无法完全复刻参考风格的限制。
- 每条偏好保存版本历史。回滚恢复上一已确认版本并生成新的当前版本，不删除历史；回滚、停用、删除只影响后续任务，不追溯修改已经生成、保存或导出的历史计划。没有历史版本时，回滚不可用。

首版不支持网页或社媒链接解析、多张参考图混合、批量照片处理、自动抓取图片，或直接修改和覆盖用户原图。

### 主要输出

1. **参考风格报告**
   - 风格名称和一句话摘要。
   - 主色调色阶卡与图例。
   - 明暗分布、对比度、饱和度、阴影/中间调/高光色彩倾向。
   - 区分可由调色迁移的特征，与源于构图、光线、场景的不可迁移特征。

2. **目标照片调色计划**
   - A、B 的匹配可行度与限制说明。
   - 按执行顺序排列的 Lightroom 参数卡片：基础校正、风格塑形、可选微调。
   - 每张卡片显示当前判断、推荐值或范围、调整原因、预期效果和副作用/风险。

3. **可选 XMP 导出**
   - 用户可下载根据结构化参数生成的 Lightroom `.xmp` 预设。
   - 只导出明确建议的全局调色项：基础明暗、白平衡、曲线、颜色混合、色彩分级和效果。
   - 不导出裁切、镜头校正、蒙版、修复等依赖具体照片的设置。
   - XMP 是 B 的起始配方，导出界面必须说明仍可能需要小范围微调。

4. **二次微调**
   - 用户按建议调整后，可上传新的 JPG 预览图。
   - Agent 基于上一轮计划对结果给出下一轮微调建议。

5. **我的调色偏好**
   - 展示全局和场景偏好档案、各条偏好来源、强度与启用状态。
   - 允许用户查看本次个性化规则如何影响参数建议，并随时修改、停用、删除或回滚。

## 5. 用户流程

1. 用户通过邮箱验证码登录，或进入新建调色任务。
2. 用户上传参考图 A 与目标照片 B，并确认 Agent 建议的照片场景（也可改用全局偏好）。
3. 系统校验文件，读取可用图像信息；RAW 同时读取 EXIF 与动态范围相关线索。
4. 系统生成 A 的风格报告，并说明风格迁移限制。
5. 系统比较 A、B，给出匹配可行度和按顺序执行的调色计划。
6. 用户选择在 Lightroom 中手动输入参数，或下载 XMP 预设后继续编辑。
7. 用户可上传调整后的预览图，获得一次或多次微调建议，并可对三个核心建议标记“采纳 / 修改后保留 / 不喜欢”。
8. 用户可选择将本轮确认的偏好写入审美档案。

## 6. 参数建议原则

### Lightroom 为主参数体系

建议覆盖：曝光、对比度、高光、阴影、白色色阶、黑色色阶、色温、色调、自然饱和度、饱和度、HSL/颜色混合、曲线、色彩分级、纹理、清晰度、去朦胧与暗角。

建议按以下顺序组织：

1. **基础校正**：白平衡、曝光与亮暗关系，先解决 B 的拍摄条件差异。
2. **风格塑形**：曲线、HSL/颜色混合、色彩分级，复现 A 的冷暖、饱和度和对比气质。
3. **可选微调**：纹理、清晰度、去朦胧、暗角等。

参数不应只有单一数值。每项应提供推荐值、可接受区间、调整理由与风险，例如：

> 曝光 `+0.35`（建议范围 `+0.20` 至 `+0.50`）：提亮人物中间调以接近参考图；若天空高光溢出，改为 `+0.20` 并优先降低高光。

### RAW 与成片格式的区别

- **RAW**：可给出更积极的白平衡、阴影与高光校正建议，并利用 EXIF 和动态范围线索判断调整空间。
- **JPG/JPEG**：提供完整计划，但应缩小高光、阴影、白平衡等高风险参数范围，提示断层、噪点或肤色失真风险。
- **PNG/WebP**：按已输出图片处理，原则同 JPEG；不假设存在可恢复的原始拍摄数据。

### 醒图映射

醒图输出保留调色意图，而非强行逐项等值翻译：

| 调色意图 | Lightroom | 醒图 |
| --- | --- | --- |
| 校正明暗 | 曝光、阴影、高光、白/黑色色阶 | 曝光、亮度、对比度、高光、阴影 |
| 建立色彩倾向 | 色温、色调、HSL、色彩分级 | 色温、色调、HSL、可用局部色彩项 |
| 塑造层次 | 参数/点曲线、纹理、清晰度、去朦胧 | 曲线、结构/清晰度等可用项 |

## 7. 系统职责

### 图像分析器

提取 A、B 的客观特征：亮度直方图、暗中亮三区占比、主色板、色相/饱和度/明度分布、色温倾向、对比度、高光与阴影裁切风险。对 RAW 额外读取 EXIF 与动态范围线索。

### 风格抽取器

将 A 的客观特征归纳成风格描述和色调配方，标记无法通过调色迁移的构图、光线或素材特征。

### 差异与规划器

比较 A 与 B，在 Lightroom 的真实编辑顺序中规划参数：先基础校正、再风格塑形、最后可选微调。

### 参数校验器

对参数施加安全范围。若检测到高光已裁切、肤色占比高、JPEG 调整空间有限等情形，则缩小建议范围或提示风险。

### 解释与导出器

将结构化计划渲染为参数卡片和 XMP。XMP 必须由确定性程序根据结构化参数生成，不能直接由语言模型生成文件内容。

模型负责审美理解、风格解释与权衡；图像测量、参数约束和 XMP 生成由确定性程序负责。

## 8. 可靠性、隐私与错误处理

- 上传前校验格式、文件大小与可解码性；对不支持或损坏的文件给出明确重传提示。
- A、B 差异过大时不强行给出复刻参数，应说明匹配受限的原因。
- 参数置信度不足时，输出区间与观察方法，不伪装成精确单值。
- XMP 导出失败时仍保留完整参数清单，确保用户可手动完成调整。
- 原始上传文件在本次处理完成后立即删除；默认不长期保存参考图文件或可识别 EXIF。
- 用户可主动保存低清预览、报告、参数和 XMP；参考图仅保存风格特征和报告，不保留原文件。
- 项目删除即时移除相关文件、预览、报告、参数、XMP 与偏好来源；账号删除请求后，备份中的最终清理在 30 天内完成。

## 9. Agent 实现方案

首版采用**单个受控的调色教练 Agent**，而不是多 Agent 协作。其职责是理解用户目标、选择分析步骤、综合工具结果、生成解释和规划下一轮微调；它不能直接修改原图，也不能绕过参数校验或用户确认生成 XMP。个人偏好仅在用户确认的场景中作为二级约束参与规划。

### Agent 工作流

1. 用户上传 A、B 后创建分析任务，并保存任务状态、文件元数据、测量结果与轮次上下文。
2. Agent 调用图像分析工具，取得 A、B 的客观视觉特征。
3. Agent 抽取 A 的风格，识别可迁移与不可迁移特征，并调用差异计算工具。
4. Agent 生成 Lightroom 调色计划的结构化草案。
5. 参数校验器根据 RAW/JPEG 类型、裁切风险、肤色风险和安全范围修正或拒绝草案。
6. Agent 将已验证计划解释为用户可读的参数卡片；用户可以手调或上传结果请求下一轮微调。
7. 只有用户明确点击导出时，后端才根据已验证参数生成 XMP。

### Agent 工具边界

| 工具 | 职责 | 执行方式 |
| --- | --- | --- |
| `analyze_image` | 提取直方图、主色、HSL、曝光风险、EXIF | 确定性图像处理程序 |
| `extract_style` | 总结 A 的风格与迁移限制 | Agent 使用测量结果判断 |
| `compare_images` | 计算 A、B 的明暗与色彩差异 | 确定性计算程序 |
| `propose_adjustments` | 规划 Lightroom 调整顺序与参数草案 | Agent，输出受约束 JSON |
| `validate_adjustments` | 校验数值、格式限制与视觉风险 | 规则引擎 |
| `generate_xmp` | 根据已验证参数生成 XMP | 确定性文件生成程序，需用户确认 |
| `review_revision` | 基于调整后预览图做下一轮微调 | Agent + 图像分析程序 |

### 结构化输出要求

Agent 的调色计划必须使用结构化数据，不以自由文本作为程序输入。每项建议至少包含 Lightroom 面板、参数名、推荐值、可接受范围、调整理由、预期效果和风险。示例：

```json
{
  "match_feasibility": "medium",
  "steps": [
    {
      "panel": "Basic",
      "parameter": "Exposure",
      "value": 0.35,
      "range": [0.2, 0.5],
      "reason": "提亮人物中间调以接近参考图",
      "risk": "天空高光可能溢出"
    }
  ],
  "warnings": ["参考图的逆光氛围不能仅靠调色完全复现"]
}
```

### Agent 运行约束

- 每轮工具调用数和总耗时设置上限，避免循环或成本失控。
- 文件生成、项目保存、删除和后续外部发布都必须通过显式用户确认门。
- 工具失败时，Agent 解释失败原因并回退到可用的分析结果，不伪造完成状态。
- 对模型输入、工具输入输出和追踪日志实施脱敏；默认不记录原图内容。
- 保存每轮计划、校验结果、用户接受或修改的参数，作为二次微调的上下文与后续评估数据。

### 个性化评估

- 对同一 A/B 可生成关闭个人偏好与开启个人偏好的两组方案，供用户盲选更符合自身审美的结果。
- 内测记录风格识别、参数建议和用户执行结果三段评分，避免将失败归因混淆。
- 测试者的匿名化参数和评分仅在明确同意后用于产品评估；不使用测试数据训练模型。

## 10. 非目标

- 不以一键成片或黑盒滤镜替代用户审美判断。
- 不保证对任何参考图的像素级复刻。
- 不处理图片抓取、社媒账号登录与版权分发。
- 不在首版提供批量选片、批量调色、个人风格长期训练或局部蒙版建议。

## 11. 验收与验证

### 结构化测试

使用已知明暗与色彩差异的样图，验证参数方向、范围和 XMP 结构正确性。

### 视觉评审

邀请有 Lightroom 使用习惯的用户，以同一组 A/B 评估：建议后的成片是否更接近参考风格、参数是否容易理解、是否愿意继续手调。

### 安全回归

RAW、JPEG、低光、强逆光、人像、风景和明显不匹配的参考图，都不得产生无解释或过度的参数建议。
