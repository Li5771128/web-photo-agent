# RefTone 调色分析 MVP 设计说明

## 产品界面目标

RefTone 是面向 Lightroom 摄影用户的匿名参考图调色工作台。页面的唯一任务是帮助用户完成：

`上传参考图 A 与目标图 B → 理解分析结果 → 手动执行 Lightroom 参数计划`

界面不提供登录、偏好管理、历史项目或场景档案。所有结果都只在当前临时会话中可见。

## 设计方向

采用流程主导的企业级 SaaS 工作台，而不是复杂的专业编辑器。页面首先告诉用户任务正处于什么状态，再在结果阶段展示足够的证据和参数，避免用户在大量控制项中迷失。

- **简洁高效**：用留白、背景层级和信息位置区分区域，尽量不用线条或连续卡片切割页面。
- **解释优先**：每个参数都有推荐值、调整原因和风险；模型结论不伪装为绝对事实。
- **临时性透明**：页首持续说明“临时会话”，新建任务时明确告知当前结果会被清除且无法回看。
- **用户控制**：用户手动在 Lightroom 中执行计划；XMP 是次级、可选的导出操作。
- **浅色结果总览**：风格结论使用白色背景、细边框和轻阴影，与工作台主体保持同一视觉层级。

## 视觉语言

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 画布 | `#F7F8FA` | 页面背景，保持轻盈和安静。 |
| 主文字 | `#19212B` | 标题、参数名和主要信息。 |
| 辅助文字 | `#697789` | 说明、元信息和低优先级提示。 |
| 主操作蓝 | `#276EF1` | 当前步骤、主按钮和推荐参数。 |
| 分析绿 | `#16866A` | 匹配可行度和正常状态。 |
| 风险橙 | `#C96A22` | 参数风险与注意事项。 |
| 柔和分组 | `#E8EDF3` / `#EDF4FF` | 不依赖边框的低对比结构层级。 |

匹配可行度条根据分数选择**单一色系**的深浅渐变，不在同一条内同时出现红、橙、绿：低分使用红系，中分使用橙系，高分使用绿系。条形填充长度对应百分比；当前静态原型在每次开始分析时随机生成 `0–100%`，用于验证不同区间的视觉效果。

正文使用系统无衬线字体；数值、流程编号和参数使用等宽字体。标题使用紧凑字距建立层级，不引入展示衬线或装饰性大字。

## 页面结构

1. **顶栏**：品牌、临时会话状态和“新建任务”。
2. **任务引导**：产品说明与三步任务导航：上传照片、查看分析中、查看结果。
3. **上传态**：并列的参考图 A 与目标图 B，显示文件类型与就绪状态；唯一主操作是“开始分析”。
4. **分析态**：只显示当前执行的一个步骤与实时百分比，不同时罗列四个步骤。
5. **结果态**：先给出风格结论、匹配可行度和 A/B 预览，再按“差异方向 → 参数计划 → 迁移风险 → 确定性读数”展示正文。

## 状态与交互

### 三步导航

| 状态 | 上传照片 | 查看分析中 | 查看结果 |
| --- | --- | --- | --- |
| 初始上传 | 可用且激活 | 禁用 | 禁用 |
| 正在分析 | 禁用 | 激活 | 禁用 |
| 已得到结果 | 可用 | 禁用 | 可用且激活 |

分析结果阶段不允许回看“查看分析中”，保证用户理解临时任务已完成，而不是把静态演示误解为可追溯任务历史。

### 分析演示

静态原型以约 3 秒模拟一次分析，四个阶段严格串行：

1. 读取素材：`0% → 100%`
2. 测量色彩：`0% → 100%`
3. 规划参数：`0% → 100%`
4. 校验风险：`0% → 100%`

任一时刻只显示一个当前阶段。最后一个阶段完成后自动进入结果态。

### 结果保护与新任务

结果是临时会话中的唯一可见结论。处于结果态时，点击“上传照片”或“新建任务”会先弹出二次确认：

- **继续查看结果**：关闭提示，不改变当前结果。
- **放弃结果并新建**：清除当前演示状态，回到上传态，并重新锁定后续步骤。

提示必须明确说明：放弃后本次结果不保留且无法回看。

### 风险说明

结果页默认只显示一段执行提示。“查看完整风险说明”在原位置展开或收起风险详情，避免将用户从参数计划带离到独立页面或弹窗。

### 直方图与色彩读数

色彩读数带展示参考图的主色序列。其下方使用 RGB 三通道半透明叠加直方图，横向从暗部到高光，借鉴 Lightroom 的照片分布阅读方式。直方图是对真实图像测量结果的展示位；当前静态页面使用示意曲线。

### 结果章节导航

宽屏在结果正文右侧提供吸附式“结果索引”，包含 A/B 差异方向、Lightroom 参数计划、迁移边界与内容风险、确定性图像读数四项。点击定位到对应章节，滚动时高亮当前位置。空间不足时，同一组导航移到结果总览下方并横向滚动；导航不包含风格结论和图片预览。

## 结果信息层级

结果态按用户从结论到执行、再到证据复核的认知顺序排列：

1. 参考风格名称、简述和匹配可行度。
2. A/B 图片预览和差异方向，说明素材之间的数值差异。
3. 参数计划：基础校正优先，随后风格塑形；每项含数值、原因和风险。
4. 迁移边界与内容风险，解释不能仅靠调色复制的部分。
5. 确定性色彩读数与 RGB 直方图，作为结论的完整复核证据。

## 响应式与可访问性

- 宽屏下上传素材和 A/B 对照并列；窄屏转为单列。
- 所有按钮保留文字标签和可见禁用状态；不以颜色作为唯一状态信号。
- 分析状态使用 `aria-live`，使当前阶段与百分比变化可被辅助技术读取。
- 二次确认使用模态对话框，并提供明确的取消和确认操作。
- 支持键盘聚焦；对减少动态效果的系统偏好应在正式应用中关闭或缩短进度过渡。

## 原型文件

- `design/mvp-flow-workbench.html`：当前 MVP 流程工作台。
- `design/mvp-variants.css`：共用视觉基础。
- `design/mvp-flow-workbench.css`：流程工作台专属状态、直方图与确认对话框样式。
- `design/mvp-variants.js`：流程状态机、串行进度、结果保护和风险展开逻辑。

## 非目标

- 登录、账户、偏好、场景档案、任务历史和跨任务个性化。
- 在浏览器中执行真实图像测量、参数校验、XMP 生成或资产删除。
- 自动修改用户原图或以生成成片替代用户手动调色。
